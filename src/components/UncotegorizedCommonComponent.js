import React, {} from 'react';
import * as PropTypes from 'prop-types';

// Any which hard move to any category

const UncotegorizedCommonComponent = (
  {}
) => {

  return (null);
};

UncotegorizedCommonComponent.propTypes = {};

UncotegorizedCommonComponent.defaultProps = {};

export default UncotegorizedCommonComponent;
