import React, {} from 'react';
import * as PropTypes from 'prop-types';

// components like links buttons, tabs and other blocks

const AnyCommonUiComponent = (
  {}
) => {

  return (null);
};

AnyCommonUiComponent.propTypes = {};

AnyCommonUiComponent.defaultProps = {};

export default AnyCommonUiComponent;
