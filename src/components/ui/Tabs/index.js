import React, {} from 'react';
import * as PropTypes from 'prop-types';
import classes from './Tabs.module.scss';


const Tabs = (
    {}
) => {

    return (
        <div>

        </div>
    );
};

Tabs.propTypes = {};

Tabs.defaultProps = {};

export default Tabs;
