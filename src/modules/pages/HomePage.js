import React from 'react';

const HomePage = (
    {}
) => {
    return (
        <main>
            <div className="container">
                home
            </div>
        </main>
    );
};

HomePage.propTypes = {};

HomePage.defaultProps = {};

export default HomePage;
